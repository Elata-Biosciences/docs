---
title: "Get config"
description: "Network info, chain id, contract addresses, supported features, fee schedule, and links to docs and source repos."
openapi: "/api-reference/openapi.yaml GET /config"
---

<Note>**Status: proposed.** Designed as the bootstrap call for SDKs (`@elata-biosciences/create-elata-demo`) and AI agents so they don't hardcode addresses or chain ids.</Note>
